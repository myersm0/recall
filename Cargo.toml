[package]
name = "recall"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
regex = "1"
dirs = "5"
libc = "0.2"
base64 = "0.22"
